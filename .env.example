# Copy to .env.local (never commit the real key)
OPENROUTER_API_KEY=
# Optional. Leave empty for a localhost-only internal API.
SCREEN_API_KEY=
# Optional. Puppeteer looks for system Chrome if unset.
CHROME_PATH=
